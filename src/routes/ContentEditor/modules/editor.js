/********************************************************
 * EditorReducer
 *
 * The EditorReducer will handle all of the state pertaining
 * to the editor. Which file is loaded, has there been changes
 * what mode its in, etc.
 *
 * @flow
 ********************************************************/
const initialState = {
  isEditing: true

}

export default function FunctionName (state = initialState, action) {
  switch (action.type) {
    default :
      return state
  }
}
